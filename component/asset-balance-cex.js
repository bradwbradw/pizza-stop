module.exports = function(params) {
        // Data: value is either null, 'like', or 'dislike'
        console.log('asset balance cex', params);
        var AssetBalanceCex = this;
        return AssetBalanceCex;
    }
